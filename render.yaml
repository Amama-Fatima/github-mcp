services:
  - type: web
    name: github-mcp
    runtime: python
    buildCommand: "uv sync"
    startCommand: "uv run fast_server.py"
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: RENDER
        value: "true"
    healthCheckPath: "/health"
    plan: free